<!-- 1. DB접속정보설정 -->
<properties resource="db.properties" />

<!-- 2. alais 별칭설정 -->
<typeAliases>
	<typeAlias type = "global.sesoc.DAO.BookListMapper" />
</typeAliases>
<!-- 3. DB접속(환경설정) 설정 -->
<environments default="development">
	<environment   id="development">
		<property name="drivers" value="${drivers}"/>
		<property name="url" value="${url}" />
		<property name="url" value="${url}" />	
		<property name="url" value="${url}" />
	</environment>
</environments>
					
<!-- 4. include할 파일을 mapper실시 -->
<mappers>
	<mapper resource=BookListMapper.xml />
</mappers>